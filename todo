**** fail safe ? while caching ***

**** refactors ****
   *  renderPages -     
      -  dublication username = Current_user.username, writer
      -   getFilePathForUser could be getUsersDataPath/ getUsersDatalocation